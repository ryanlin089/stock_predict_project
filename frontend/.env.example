# Leave empty when the frontend and backend are served from the same domain
# or when API_PROXY_BASE_URL is configured for Next.js rewrites.
NEXT_PUBLIC_API_BASE_URL=

# Used by Next.js rewrites on server-capable hosts such as Vercel.
API_PROXY_BASE_URL=http://localhost:8000
